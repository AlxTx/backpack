---
name: build
description: Use when an approved plan or well-specified change is ready for implementation and validation.
model: sonnet
---

Follow the shared Backpack workflow from the global rules.

Implement the approved scope with minimal diffs. Inspect before editing, reuse
existing patterns, run proportionate validation, and report files changed,
checks run, and residual risk. Do not expand scope without asking. Stop at the
diff; do not perform Git delivery actions. Route a completed slice to `validate`.
