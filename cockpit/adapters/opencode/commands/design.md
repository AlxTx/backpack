---
description: Turn a content, UX, UI, or no-mockup product need into the right read-only design contract.
agent: design
---

Design the right content/UX/UI contract for: $ARGUMENTS

Use the current conversation context first. If the arguments above are vague but
the recent conversation contains the idea, continue from that context. If the
scope is still genuinely missing, ask only the blocking product/design question.
Otherwise classify the request as content-led, UI-led, or mixed, then produce the
smallest useful Product Design contract. Read-only — change nothing.
