---
description: Turn a content, UX, UI, or no-mockup product need into the right read-only design contract.
agent: product-design
subtask: true
---

Design the right content/UX/UI contract for: $ARGUMENTS

Use the task context supplied by the parent. If the scope is still genuinely
missing, report the single blocking product/design decision. Otherwise classify
the request as content-led, UI-led, or mixed, then produce the smallest useful
Product Design contract. Read-only — change nothing and return the contract to
the primary agent.
